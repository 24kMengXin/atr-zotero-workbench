[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "atr-zotero-workbench"
version = "0.1.0"
description = "Traceable ATR evidence graph and Zotero import/sync bridge"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
